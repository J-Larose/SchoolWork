#include <stdio.h>

#define ARR_MAX 16

int main(){
  int num;
  int count = 0;
  int arr[ARR_MAX];

  while(count >= 0){
    scanf("%d",&num);
    // addToArray(num);
    count++;
  }
  // printArray(arr[]);
  
  printf("test\n");
   return 0;

}
